# StockLenses Backend — environment variables
# Copy this file to .env and fill in your keys.
# Load with: export $(cat .env | xargs)
# Or install python-dotenv and add load_dotenv() to backend/main.py

# ── Finnhub (required for fundamentals ETL) ────────────────────────────────
# Get a free key at https://finnhub.io
FINNHUB_API_KEY=your_finnhub_api_key_here

# ── Yahoo Finance session (required for price + fundamentals ETL) ───────────
# The Yahoo client uses a session cookie obtained from fc.yahoo.com.
# No key needed — but the host running this must have outbound HTTPS access
# to fc.yahoo.com and finance.yahoo.com (not blocked by firewall/VPN).
# If the session bootstrap fails, run the server without a VPN.
